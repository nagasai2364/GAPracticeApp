name: Naga 

on: workflow_dispatch

env:
    Day : Wednesday
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Naga steps
        run: echo "Naga workflow executed"

      - name: Today's day
        run: echo "Today is ${{env.Day}}"

      - name: User Details
        run: echo "The name of the User is ${{vars.USER}}"

      - name: User Age
        run: echo "The age of ${{vars.USER}} is ${{vars.AGE}}"
